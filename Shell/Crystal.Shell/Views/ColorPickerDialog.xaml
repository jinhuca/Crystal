<Window x:Class="Crystal.Shell.Views.ColorPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Edit colors"
        Width="560" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="Black"
        BorderBrush="{StaticResource AccentBrush}" BorderThickness="1"
        Foreground="{StaticResource TextBrush}"
        FontFamily="{StaticResource UiFontFamily}">
  <Window.Resources>
    <Style x:Key="CaptionButtonStyle" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Width" Value="46"/>
      <Setter Property="Height" Value="30"/>
      <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource DividerBrush}"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="FieldTextBoxStyle" TargetType="TextBox">
      <Setter Property="Height" Value="30"/>
      <Setter Property="Background" Value="#1E2A38"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="CaretBrush" Value="{StaticResource TextBrush}"/>
      <Setter Property="SelectionBrush" Value="{StaticResource AccentBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="Padding" Value="8,0"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="VerticalContentAlignment" Value="Center"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Style.Triggers>
        <Trigger Property="IsKeyboardFocusWithin" Value="True">
          <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="DialogButtonStyle" TargetType="Button">
      <Setter Property="MinWidth" Value="84"/>
      <Setter Property="Height" Value="30"/>
      <Setter Property="Margin" Value="8,0,0,0"/>
      <Setter Property="Padding" Value="14,4"/>
      <Setter Property="FontSize" Value="13"/>
      <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
      <Setter Property="Background" Value="#1E2A38"/>
      <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="SnapsToDevicePixels" Value="True"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                Margin="{TemplateBinding Padding}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#26394D"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#17324B"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="PrimaryDialogButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
      <Setter Property="Background" Value="{StaticResource AccentSelectionBrush}"/>
      <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>
  </Window.Resources>

  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="36" GlassFrameThickness="0" CornerRadius="0"
                        ResizeBorderThickness="0" UseAeroCaptionButtons="False"/>
  </shell:WindowChrome.WindowChrome>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="36"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Title bar -->
    <Grid Grid.Row="0" Background="Black">
      <TextBlock Text="Edit colors" Foreground="{StaticResource LabelBrush}" FontSize="14"
                 VerticalAlignment="Center" HorizontalAlignment="Left" Margin="12,0,0,0"/>
      <Button HorizontalAlignment="Right" Style="{StaticResource CaptionButtonStyle}"
              Click="OnCancelClick" Content="&#xE8BB;" ToolTip="Close"/>
    </Grid>

    <!-- Picker body -->
    <Grid Grid.Row="1" Margin="16,12,16,4">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <!-- Hue (x) × Saturation (y) field with a value overlay. -->
      <Border Grid.Column="0" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
              SnapsToDevicePixels="True">
        <Grid x:Name="SvArea" Width="300" Height="240"
              MouseLeftButtonDown="OnSvMouseDown" MouseMove="OnSvMouseMove"
              MouseLeftButtonUp="OnSvMouseUp" Background="Transparent">
          <Rectangle>
            <Rectangle.Fill>
              <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#FF0000" Offset="0.000"/>
                <GradientStop Color="#FFFF00" Offset="0.166"/>
                <GradientStop Color="#00FF00" Offset="0.333"/>
                <GradientStop Color="#00FFFF" Offset="0.500"/>
                <GradientStop Color="#0000FF" Offset="0.666"/>
                <GradientStop Color="#FF00FF" Offset="0.833"/>
                <GradientStop Color="#FF0000" Offset="1.000"/>
              </LinearGradientBrush>
            </Rectangle.Fill>
          </Rectangle>
          <Rectangle>
            <Rectangle.Fill>
              <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#00FFFFFF" Offset="0"/>
                <GradientStop Color="#FFFFFFFF" Offset="1"/>
              </LinearGradientBrush>
            </Rectangle.Fill>
          </Rectangle>
          <Rectangle x:Name="SvValueOverlay" Fill="Black" Opacity="0"/>
          <Canvas x:Name="SvThumbLayer" IsHitTestVisible="False">
            <Ellipse x:Name="SvThumb" Width="14" Height="14" StrokeThickness="2" Stroke="White">
              <Ellipse.Effect>
                <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="0" Opacity="0.8"/>
              </Ellipse.Effect>
            </Ellipse>
          </Canvas>
        </Grid>
      </Border>

      <!-- Preview swatch -->
      <Border Grid.Column="1" Width="44" Height="240" Margin="12,0,0,0"
              BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" SnapsToDevicePixels="True">
        <Rectangle x:Name="PreviewSwatch"/>
      </Border>

      <!-- Brightness (value) slider -->
      <Border Grid.Column="2" Width="22" Height="240" Margin="12,0,0,0"
              BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" SnapsToDevicePixels="True">
        <Grid x:Name="ValueBar" MouseLeftButtonDown="OnValueMouseDown" MouseMove="OnValueMouseMove"
              MouseLeftButtonUp="OnValueMouseUp" Background="Transparent">
          <Rectangle x:Name="ValueBarFill"/>
          <Canvas x:Name="ValueThumbLayer" IsHitTestVisible="False">
            <Border x:Name="ValueThumb" Width="26" Height="6" Margin="-3,0,0,0"
                    Background="Transparent" BorderBrush="White" BorderThickness="2">
              <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="3" ShadowDepth="0" Opacity="0.8"/>
              </Border.Effect>
            </Border>
          </Canvas>
        </Grid>
      </Border>

      <!-- Hex + RGB inputs -->
      <StackPanel Grid.Column="3" Margin="16,0,0,0" VerticalAlignment="Top">
        <TextBox x:Name="HexBox" Style="{StaticResource FieldTextBoxStyle}" MaxLength="7"
                 LostFocus="OnHexCommitted" KeyDown="OnHexKeyDown"/>
        <Grid Margin="0,10,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBox x:Name="RedBox" Grid.Column="0" Style="{StaticResource FieldTextBoxStyle}"
                   MaxLength="3" TextChanged="OnRgbChanged"/>
          <TextBlock Grid.Column="1" Text="Red" Foreground="{StaticResource LabelBrush}"
                     VerticalAlignment="Center" Margin="10,0,0,0"/>
        </Grid>
        <Grid Margin="0,10,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBox x:Name="GreenBox" Grid.Column="0" Style="{StaticResource FieldTextBoxStyle}"
                   MaxLength="3" TextChanged="OnRgbChanged"/>
          <TextBlock Grid.Column="1" Text="Green" Foreground="{StaticResource LabelBrush}"
                     VerticalAlignment="Center" Margin="10,0,0,0"/>
        </Grid>
        <Grid Margin="0,10,0,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBox x:Name="BlueBox" Grid.Column="0" Style="{StaticResource FieldTextBoxStyle}"
                   MaxLength="3" TextChanged="OnRgbChanged"/>
          <TextBlock Grid.Column="1" Text="Blue" Foreground="{StaticResource LabelBrush}"
                     VerticalAlignment="Center" Margin="10,0,0,0"/>
        </Grid>
      </StackPanel>
    </Grid>

    <!-- OK / Cancel -->
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right"
                Margin="16,12,16,14">
      <Button Content="Cancel" Style="{StaticResource DialogButtonStyle}" Click="OnCancelClick"/>
      <Button Content="OK" Style="{StaticResource PrimaryDialogButtonStyle}" Click="OnOkClick"/>
    </StackPanel>
  </Grid>
</Window>
