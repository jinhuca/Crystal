<UserControl x:Class="BiosModule.Views.BiosDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100"
             Background="#101317" Foreground="White"
             FontFamily="Segoe UI">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Button Grid.Column="0" Content="&#x2190; Back" Padding="14,6"
              Background="#1E2A38" Foreground="White" BorderThickness="0"
              Command="{Binding ShowDashboardCommand}"/>
      <TextBlock Grid.Column="1" Text="BIOS" Foreground="#3E9BE8" FontSize="28"
                 FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0"/>
    </Grid>

    <Border Grid.Row="1" VerticalAlignment="Top" Padding="24" Background="#161B22" CornerRadius="8">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="220"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Manufacturer" Foreground="#8A94A0" Margin="0,6"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Manufacturer}" Margin="0,6"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Version" Foreground="#8A94A0" Margin="0,6"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Version}" Margin="0,6"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Release date" Foreground="#8A94A0" Margin="0,6"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding ReleaseDate}" Margin="0,6"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="SMBIOS version" Foreground="#8A94A0" Margin="0,6"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SmbiosSpecVersion}" Margin="0,6"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Serial number" Foreground="#8A94A0" Margin="0,6"/>
        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SerialNumber}" Margin="0,6"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Status" Foreground="#8A94A0" Margin="0,6"/>
        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Status}" Margin="0,6"/>
      </Grid>
    </Border>
  </Grid>
</UserControl>
