<UserControl x:Class="Crystal.GpuModule.Views.SummaryViews.Gpu3DView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Crystal.GpuModule.Views.SummaryViews"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:graphs="clr-namespace:Crystal.Controls.PerformanceGraphs;assembly=Crystal.Controls"
             mc:Ignorable="d"
             d:DesignHeight="88" d:DesignWidth="130">
  <!-- DataContext is a GpuAdapterViewModel, inherited from the per-adapter block. Header (title left,
       live value right) over a value-banded history graph of the 3D-engine utilization (0-100%);
       self-registers so the view model feeds it on each poll. -->
  <!-- Left-aligned so the 130-wide content sits at the left of the wider column it shares with the
       Memory tile (170), keeping its graph aligned with Memory's left-pinned graph above. -->
  <!-- Graph replaced with a header-only value+unit readout plus a trend glyph (test/minimal-graphs). -->
  <UserControl.Resources>
    <ResourceDictionary Source="/Crystal.Controls;component/Metrics/MetricEnrichmentResources.xaml"/>
  </UserControl.Resources>
  <Grid Width="{x:Static local:GpuSummaryViewConstants.GraphWidth}" VerticalAlignment="Stretch">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="3D" Style="{StaticResource MetricCaption}"
               Foreground="{StaticResource TextBrush}" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0,0,0,4"/>

    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
        <TextBlock Text="{Binding Load3D, StringFormat={}{0:0.00}}" Style="{StaticResource TileMetricValue}"/>
        <TextBlock Text="%" Style="{StaticResource MetricUnit}"/>
        <TextBlock Text="{Binding Load3DRow.Trend, Converter={StaticResource TrendToGlyph}}"
                   Foreground="{Binding Load3DRow.Trend, Converter={StaticResource TrendToBrush}}"
                   Style="{StaticResource MetricUnit}" Margin="4,0,0,0"/>
      </StackPanel>
      <TextBlock Style="{StaticResource MetricStatLine}">
        <Run Text="min "/><Run Text="{Binding Load3DRow.Min, StringFormat={}{0:0}}"/>
        <Run Text="   avg "/><Run Text="{Binding Load3DRow.Avg, StringFormat={}{0:0}}"/>
        <Run Text="   max "/><Run Text="{Binding Load3DRow.Max, StringFormat={}{0:0}}"/>
      </TextBlock>
    </StackPanel>
  </Grid>
</UserControl>
