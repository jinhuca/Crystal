<UserControl x:Class="Crystal.GpuModule.Views.GpuSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:Crystal.GpuModule.Views"
             xmlns:sv="clr-namespace:Crystal.GpuModule.Views.SummaryViews"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="1000"
             Background="Transparent" Foreground="{StaticResource TextBrush}"
             FontFamily="{StaticResource UiFontFamily}">
  <!-- One tile per detected adapter, each an identity header (kind + name + physical location) over a
       meter grid. The integrated GPU is a 3x2 grid — Temperature/Memory Used/Utilization over
       Power/3D/Info; each dedicated GPU is a 4x2 grid — Temperature/Memory Used/Clock/Utilization
       over Hot Spot/3D/Power/Info, with PCIe Rx/Tx throughput shown inline on the identity header
       (the full PCIe history graphs live in GpuDetailView). "Info" restates the static specs from
       GpuDetailView. A single
       adapter renders as one tile with no border and no ScrollViewer; several adapters render as a
       horizontally scrollable strip (wheel scrolls sideways, scrollbars hidden) where tiles after the
       first get a left divider. The per-metric tiles live in Views/SummaryViews. -->
  <UserControl.Resources>
    <local:AdapterTileChromeConverter x:Key="TileChrome"/>

    <!-- Integrated adapter tile: an identity header over a 3x2 meter grid. Row 2 is
         Temperature/Memory Used/Utilization; row 3 is Power/3D/static-specs Info. -->
    <DataTemplate x:Key="IntegratedTile">
      <Border Margin="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource TileChrome}}"
              Padding="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource TileChrome}}"
              BorderBrush="{StaticResource DividerBrush}"
              BorderThickness="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource TileChrome}, ConverterParameter=border}">
        <Grid VerticalAlignment="Stretch" ShowGridLines="False">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <!-- Row 1: adapter identity — "Integrated GPU" accent label + product name + physical location. -->
          <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="{Binding KindLabel}" Style="{StaticResource TileTitleCaption}"/>
            <TextBlock Text="{Binding Name}" Style="{StaticResource TileTitleBrand}" Margin="8,0,0,0"
                       TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}"/>
            <TextBlock Text="{Binding PhysicalLocation, TargetNullValue=—}" Foreground="{StaticResource LabelBrush}"
                       FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center" Margin="12,0,0,0"/>
          </StackPanel>

          <sv:GpuTemperatureView Grid.Row="1" Grid.Column="0" Margin="0,0,8,8"/>
          <sv:GpuMemoryView      Grid.Row="1" Grid.Column="1" Margin="0,0,8,8"/>
          <sv:GpuUtilizationView Grid.Row="1" Grid.Column="2" Margin="0,0,0,8" VerticalAlignment="Stretch"/>
          <sv:GpuPowerView       Grid.Row="2" Grid.Column="0" Margin="0,0,8,0"/>
          <sv:Gpu3DView          Grid.Row="2" Grid.Column="1" Margin="0,0,8,0"/>
          <sv:GpuInfoView        Grid.Row="2" Grid.Column="2" Margin="0,0,0,0"/>
        </Grid>
      </Border>
    </DataTemplate>

    <!-- Dedicated adapter tile: an identity header (with inline PCIe Rx/Tx throughput) over a 4x2
         meter grid. Row 2 is Temperature/Memory Used/Clock/Utilization; row 3 is Hot Spot/3D/Power/Info.
         Clock and Power carry their value/min/max breakdown tables on the right (CPU-tile style). -->
    <DataTemplate x:Key="DedicatedTile">
      <Border Margin="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource TileChrome}}"
              Padding="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource TileChrome}}"
              BorderBrush="{StaticResource DividerBrush}"
              BorderThickness="{Binding (ItemsControl.AlternationIndex), RelativeSource={RelativeSource AncestorType=ContentPresenter}, Converter={StaticResource TileChrome}, ConverterParameter=border}">
        <!-- The tile sizes to content; each metric view carries its own fixed width (see
             GpuSummaryViewConstants) so nothing can balloon in the infinite-width strip. -->
        <Grid VerticalAlignment="Stretch">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <!-- Row 1: adapter identity — "Dedicated GPU" accent label + product name + physical location on
               the left; the live PCIe Rx (down-triangle) and PCIe Tx (up-triangle) throughput docked to
               the right edge of the tile. -->
          <DockPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="4" Margin="0,0,0,8">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
              <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Path Data="M0,0 L10,0 L5,7 Z" Fill="#3BD15A" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Text="PCIe Rx" Foreground="{StaticResource LabelBrush}" FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding PcieRxMBps, StringFormat={}{0:0.0}, TargetNullValue=0.0}" Foreground="{StaticResource TextBrush}" FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBlock Text="MB/s" Foreground="{StaticResource LabelBrush}" FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center" Margin="4,0,0,0"/>
              </StackPanel>
              <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="24,0,0,0">
                <Path Data="M0,7 L10,7 L5,0 Z" Fill="#F0A030" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBlock Text="PCIe Tx" Foreground="{StaticResource LabelBrush}" FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding PcieTxMBps, StringFormat={}{0:0.0}, TargetNullValue=0.0}" Foreground="{StaticResource TextBrush}" FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBlock Text="MB/s" Foreground="{StaticResource LabelBrush}" FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center" Margin="4,0,0,0"/>
              </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <TextBlock Text="{Binding KindLabel}" Style="{StaticResource TileTitleCaption}"/>
              <TextBlock Text="{Binding Name}" Style="{StaticResource TileTitleBrand}" Margin="8,0,0,0"
                         TextTrimming="CharacterEllipsis" ToolTip="{Binding Name}"/>
              <TextBlock Text="{Binding PhysicalLocation, TargetNullValue=—}" Foreground="{StaticResource LabelBrush}"
                         FontSize="{StaticResource CaptionFontSize}" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
          </DockPanel>

          <sv:GpuTemperatureView Grid.Row="1" Grid.Column="0" Margin="0,0,8,8"/>
          <sv:GpuMemoryView      Grid.Row="1" Grid.Column="1" Margin="0,0,8,8"/>
          <sv:GpuClockView       Grid.Row="1" Grid.Column="2" Margin="0,0,8,8"/>
          <sv:GpuUtilizationView Grid.Row="1" Grid.Column="3" Margin="0,0,0,8" VerticalAlignment="Stretch"/>

          <sv:GpuHotSpotView     Grid.Row="2" Grid.Column="0" Margin="0,0,8,0"/>
          <sv:Gpu3DView          Grid.Row="2" Grid.Column="1" Margin="0,0,8,0"/>
          <sv:GpuPowerView       Grid.Row="2" Grid.Column="2" Margin="0,0,8,0"/>
          <sv:GpuInfoView        Grid.Row="2" Grid.Column="3" Margin="0,0,0,0"/>
        </Grid>
      </Border>
    </DataTemplate>

    <local:GpuAdapterTileTemplateSelector x:Key="TileSelector"
                                          IntegratedTemplate="{StaticResource IntegratedTile}"
                                          DedicatedTemplate="{StaticResource DedicatedTile}"/>

    <!-- One adapter: render the tile directly with no ScrollViewer, so none of the scroll machinery is
         built. The 10px left inset matches CpuSummaryView so the GPU tiles line up with the CPU tiles. -->
    <DataTemplate x:Key="SingleAdapterTemplate">
      <ContentControl Margin="10,0,0,0" Content="{Binding Adapters[0]}"
                      ContentTemplateSelector="{StaticResource TileSelector}"
                      VerticalAlignment="Stretch" VerticalContentAlignment="Stretch"/>
    </DataTemplate>

    <!-- Several adapters: a horizontally scrollable strip (scrollbars hidden, wheel scrolls sideways),
         same 10px inset as above. -->
    <DataTemplate x:Key="AdapterStripTemplate">
      <ScrollViewer Margin="10,0,0,0"
                    HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled"
                    PreviewMouseWheel="OnScrollHorizontally">
        <ItemsControl ItemsSource="{Binding Adapters}" AlternationCount="99"
                      ItemTemplateSelector="{StaticResource TileSelector}" VerticalAlignment="Top">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
        </ItemsControl>
      </ScrollViewer>
    </DataTemplate>
  </UserControl.Resources>

  <Border Background="Transparent">
    <Grid Margin="5" HorizontalAlignment="Left" VerticalAlignment="Stretch" ShowGridLines="False">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>

      <!-- Section title: accent tick + "GPU". -->
      <Border Grid.Row="0" BorderBrush="{StaticResource DividerBrush}" BorderThickness="0,0,0,1"
              Padding="0,0,0,4" Margin="0,0,0,6">
        <StackPanel Orientation="Horizontal">
          <Border Width="3" Background="{StaticResource AccentBrush}" Margin="0,1,8,1"/>
          <TextBlock Text="GPU" Foreground="{StaticResource ValueBrush}" FontSize="{StaticResource BodyFontSize}" FontWeight="SemiBold"
                     VerticalAlignment="Center"/>
        </StackPanel>
      </Border>

      <!-- A lone adapter renders as a single tile with no ScrollViewer (SingleAdapterTemplate); several
           adapters render as a horizontally scrollable strip (AdapterStripTemplate). Swapping the
           ContentTemplate means only the active branch is instantiated, so the single-adapter case
           builds none of the scroll machinery. -->
      <ContentControl Grid.Row="1" Content="{Binding}" VerticalAlignment="Center" VerticalContentAlignment="Stretch">
        <ContentControl.Style>
          <Style TargetType="ContentControl">
            <Setter Property="ContentTemplate" Value="{StaticResource AdapterStripTemplate}"/>
            <Style.Triggers>
              <DataTrigger Binding="{Binding Adapters.Count}" Value="1">
                <Setter Property="ContentTemplate" Value="{StaticResource SingleAdapterTemplate}"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </ContentControl.Style>
      </ContentControl>
    </Grid>
  </Border>
</UserControl>
