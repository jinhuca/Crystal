<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Palette merged into this dictionary's own scope so the StaticResource brush keys resolve
       from styles defined here (a sibling merged dictionary is never searched). -->
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/Crystal.Resources;component/Styles/Palette.xaml"/>
  </ResourceDictionary.MergedDictionaries>

  <!-- Flat dark button matching the tile palette: neutral dark fill, grey border, accent-blue on
       press, and a strip of the stock light-blue default-button chrome. Shared by the process
       toolbar (ToolbarButtonStyle) and the Run-new-task dialog (DialogButtonStyle) so the two read
       identically and only change in one place. Sizing/typography are left to the derived styles. -->
  <Style x:Key="FlatDarkButtonStyle" TargetType="Button">
    <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
    <Setter Property="Background" Value="#1C1C1C"/>
    <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  SnapsToDevicePixels="True">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                              Margin="{TemplateBinding Padding}" RecognizesAccessKey="True"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource DividerBrush}"/>
              <Setter TargetName="Bd" Property="BorderBrush" Value="#4A4A4A"/>
              <Setter Property="Foreground" Value="{StaticResource ValueBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
              <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
              <Setter Property="Foreground" Value="#0A0A0A"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter TargetName="Bd" Property="Background" Value="#161616"/>
              <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource DividerBrush}"/>
              <Setter Property="Foreground" Value="#5A6470"/>
              <Setter Property="Cursor" Value="Arrow"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
