<UserControl x:Class="Crystal.CpuModule.Views.DetailViews.CpuInstructionSetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="800"
             Foreground="{StaticResource TextBrush}" FontFamily="{StaticResource UiFontFamily}">
  <!-- DataContext is the CPU specs view model, set by the host layout. The instruction-set feature
       grid: each ISA extension lit green when available, dimmed when not. -->
  <StackPanel>
    <TextBlock Text="Instruction Set Features Available:" Foreground="{StaticResource TextMutedBrush}"
               FontSize="{StaticResource SubHeadingFontSize}" Margin="0,0,0,6"/>
    <ItemsControl ItemsSource="{Binding InstructionSet}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel Orientation="Vertical" Height="90"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <TextBlock Text="{Binding Name}" FontSize="{StaticResource CaptionFontSize}" FontWeight="SemiBold"
                     Width="96" Margin="0,1,12,1">
            <TextBlock.Style>
              <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="#4A4A4A"/>
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsAvailable}" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource OkBrush}"/>
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </TextBlock.Style>
          </TextBlock>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </StackPanel>
</UserControl>
