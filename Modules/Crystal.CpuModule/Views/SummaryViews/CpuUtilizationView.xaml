<UserControl x:Class="Crystal.CpuModule.Views.SummaryViews.CpuUtilizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:graphs="clr-namespace:Crystal.Controls.PerformanceGraphs;assembly=Crystal.Controls"
             mc:Ignorable="d"
             d:DesignHeight="90" d:DesignWidth="320">
  <!-- DataContext is the CPU SensorsViewModel, inherited from the host tile. -->
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <graphs:PerformanceGraph x:Name="CpuUtilizationGraph" Grid.Column="0"
                             graphs:GraphIdentity.Id="Cpu.Utilization"
                             MinValue="0" MaxValue="100"
                             MinHeight="{StaticResource SummaryGraphMinHeight}"/>
    <StackPanel Grid.Column="1" Style="{StaticResource SummaryStackPanelStyle}">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding Load, StringFormat={}{0:0.00}}" Style="{StaticResource MetricValue}"/>
        <TextBlock Text="%" Style="{StaticResource MetricUnit}"/>
      </StackPanel>
      <TextBlock Text="Utilization" Style="{StaticResource MetricCaption}"/>
      <!-- Package idle (C-state) residency; Intel MSR-sourced, hidden per state when not exposed. -->
      <TextBlock Text="{Binding PackageC2Pct, StringFormat=C2 {0:0}%}">
        <TextBlock.Style>
          <Style TargetType="TextBlock" BasedOn="{StaticResource MetricCaption}">
            <Style.Triggers>
              <DataTrigger Binding="{Binding PackageC2Pct}" Value="0">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>
      <TextBlock Text="{Binding PackageC3Pct, StringFormat=C3 {0:0}%}">
        <TextBlock.Style>
          <Style TargetType="TextBlock" BasedOn="{StaticResource MetricCaption}">
            <Style.Triggers>
              <DataTrigger Binding="{Binding PackageC3Pct}" Value="0">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>
      <TextBlock Text="{Binding PackageC6Pct, StringFormat=C6 {0:0}%}">
        <TextBlock.Style>
          <Style TargetType="TextBlock" BasedOn="{StaticResource MetricCaption}">
            <Style.Triggers>
              <DataTrigger Binding="{Binding PackageC6Pct}" Value="0">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>
      <TextBlock Text="{Binding PackageC7Pct, StringFormat=C7 {0:0}%}">
        <TextBlock.Style>
          <Style TargetType="TextBlock" BasedOn="{StaticResource MetricCaption}">
            <Style.Triggers>
              <DataTrigger Binding="{Binding PackageC7Pct}" Value="0">
                <Setter Property="Visibility" Value="Collapsed"/>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </TextBlock.Style>
      </TextBlock>
    </StackPanel>
  </Grid>
</UserControl>
