<UserControl x:Class="Crystal.CpuModule.Views.SummaryViews.CpuUtilizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:graphs="clr-namespace:Crystal.Controls.PerformanceGraphs;assembly=Crystal.Controls"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="500">
  <!-- DataContext is the CPU SensorsViewModel, inherited from the host tile.
       The full CPU-utilization history graph; self-registers so the view model feeds it on each poll. -->
  <Grid VerticalAlignment="Stretch" HorizontalAlignment="Center" Background="Transparent" ShowGridLines="False">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="100"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Grid.Column="0" Text="Utilization" HorizontalAlignment="Right" VerticalAlignment="Center"
               Margin="0,0,12,0"
               Style="{StaticResource MetricCaption}"/>
    <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
      <TextBlock Text="{Binding Load, StringFormat={}{0:0.00}}" Style="{StaticResource UtilizationCaption}" VerticalAlignment="Bottom"/>
      <TextBlock Text="%" Style="{StaticResource MetricUnit}" VerticalAlignment="Bottom"/>
    </StackPanel>
    
    <graphs:PerformanceGraphLite x:Name="CpuUtilizationGraph" Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1"
                                 graphs:GraphIdentity.Id="Cpu.Utilization"
                                 Style="{StaticResource SquareDotGraphLiteStyle}"
                                 Capacity="120" MinValue="0" MaxValue="100"
                                 VerticalAlignment="Bottom"/>


  </Grid>
</UserControl>
