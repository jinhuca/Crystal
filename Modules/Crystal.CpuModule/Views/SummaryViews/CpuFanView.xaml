<UserControl x:Class="Crystal.CpuModule.Views.SummaryViews.CpuFanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:graphs="clr-namespace:Crystal.Controls.PerformanceGraphs;assembly=Crystal.Controls"
             mc:Ignorable="d"
             d:DesignHeight="90" d:DesignWidth="320">
  <!-- DataContext is the CPU SensorsViewModel, inherited from the host tile. -->
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
    </Grid.ColumnDefinitions>
    <graphs:PerformanceGraphLite x:Name="CpuFanGraph" Grid.Column="0"
                                 graphs:GraphIdentity.Id="Cpu.Fan"
                                 Style="{StaticResource SquareDotGraphLiteStyle}"
                                 Margin="0,1,0,1" MinHeight="{StaticResource SummaryGraphMinHeight}"
                                 MaxValue="{Binding FanGraphMax}"/>
    <StackPanel Grid.Column="1" Style="{StaticResource SummaryStackPanelStyle}">
      <StackPanel Orientation="Horizontal">
        <TextBlock Text="{Binding FanReadoutText}" Style="{StaticResource MetricValue}"/>
        <TextBlock Text="{Binding FanReadoutUnit}" Style="{StaticResource MetricUnit}"/>
      </StackPanel>
      <TextBlock Text="CPU Fan" Style="{StaticResource MetricCaption}"/>
    </StackPanel>
  </Grid>
</UserControl>
