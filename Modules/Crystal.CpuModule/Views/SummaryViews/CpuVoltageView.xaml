<UserControl x:Class="Crystal.CpuModule.Views.SummaryViews.CpuVoltageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:graphs="clr-namespace:Crystal.Controls.PerformanceGraphs;assembly=Crystal.Controls"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="260">
  <!-- DataContext is the CPU SensorsViewModel, inherited from the host tile. Voltage history graph,
       titled in the utilization pattern: metric label + inline SoC voltage (left), live value
       (right). Self-registers under "Cpu.Voltage" so the view model feeds it on each poll. -->
  <Grid VerticalAlignment="Stretch" Background="Transparent">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,4">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Bottom">
        <TextBlock Text="Voltage" Style="{StaticResource MetricCaption}" Foreground="{StaticResource TextBrush}"/>
        <TextBlock Text="{Binding SocVoltage, StringFormat=SoC {0:0.00} V}" Style="{StaticResource MetricCaption}" Margin="10,0,0,0"/>
      </StackPanel>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
        <TextBlock Text="{Binding Voltage, StringFormat={}{0:0.00}}" Style="{StaticResource UtilizationCaption}" VerticalAlignment="Bottom"/>
        <TextBlock Text="Volt" Style="{StaticResource MetricUnit}" VerticalAlignment="Bottom"/>
      </StackPanel>
    </Grid>

    <graphs:AdaptiveGraph x:Name="VoltageGraph" Grid.Row="1"
                          graphs:GraphIdentity.Id="Cpu.Voltage"
                          Capacity="120" MinValue="0" MaxValue="2"
                          Accent="#3BD15A" ShowChrome="True" MinHeight="40"
                          CellPitch="{StaticResource DashboardDotPitch}"/>
  </Grid>
</UserControl>
