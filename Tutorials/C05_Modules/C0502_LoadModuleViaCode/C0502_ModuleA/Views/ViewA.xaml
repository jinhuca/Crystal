<UserControl x:Class="C0502_ModuleA.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:crystal="clr-namespace:Crystal;assembly=Crystal"
             crystal:ViewModelLocator.AutoWireViewModel="True">
	<Grid Background="AntiqueWhite">
    <Grid.RowDefinitions>
      <RowDefinition Height="45"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="35"/>
    </Grid.RowDefinitions>
    <TextBlock Text="View A" FontSize="18" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Blue"/>
    <TextBlock Grid.Row="1" FontSize="48" FontWeight="Black" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Red" Text="{Binding CountOfItems}"/>
    <Button Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Add" FontSize="31" Command="{Binding AddCountCommand}"/>
    <CheckBox Grid.Row="3" Content="IsEnabled" IsChecked="{Binding IsEnabled}"/>
	</Grid>
</UserControl>
