<UserControl x:Class="StorageModule.Views.StorageDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="1100"
             Background="#101317" Foreground="White"
             FontFamily="Segoe UI">
  <Grid Margin="24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid Grid.Row="0" Margin="0,0,0,16">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Button Grid.Column="0" Content="&#x2190; Back" Padding="14,6"
              Background="#1E2A38" Foreground="White" BorderThickness="0"
              Command="{Binding ShowDashboardCommand}"/>
      <TextBlock Grid.Column="1" Text="Storage" Foreground="#3E9BE8" FontSize="28"
                 FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0"/>
    </Grid>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
      <StackPanel Margin="0,0,48,0">
        <TextBlock Text="{Binding TotalCapacityLabel}" FontSize="30" FontWeight="Light"/>
        <TextBlock Text="Total capacity" Foreground="#8A94A0" FontSize="12"/>
      </StackPanel>
      <StackPanel>
        <TextBlock Text="{Binding DriveCountLabel}" FontSize="30" FontWeight="Light"/>
        <TextBlock Text="Drives" Foreground="#8A94A0" FontSize="12"/>
      </StackPanel>
    </StackPanel>

    <ItemsControl Grid.Row="2" ItemsSource="{Binding Drives}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <Border Margin="0,0,0,10" Padding="16" Background="#161B22" CornerRadius="8">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
              </Grid.RowDefinitions>
              <StackPanel Grid.Row="0" Orientation="Horizontal">
                <TextBlock Text="{Binding Model}" Foreground="#3E9BE8" FontSize="16"
                           FontWeight="SemiBold" Width="320" TextTrimming="CharacterEllipsis"/>
                <TextBlock Text="{Binding CapacityLabel}" FontSize="16" Width="120"/>
                <TextBlock Text="{Binding MediaType}" Foreground="#C8C8C8"/>
              </StackPanel>
              <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                <StackPanel Width="180">
                  <TextBlock Text="Interface" Foreground="#8A94A0" FontSize="11"/>
                  <TextBlock Text="{Binding InterfaceType}"/>
                </StackPanel>
                <StackPanel Width="180">
                  <TextBlock Text="Partitions" Foreground="#8A94A0" FontSize="11"/>
                  <TextBlock Text="{Binding PartitionsLabel}"/>
                </StackPanel>
                <StackPanel Width="220">
                  <TextBlock Text="Firmware" Foreground="#8A94A0" FontSize="11"/>
                  <TextBlock Text="{Binding FirmwareRevision}"/>
                </StackPanel>
                <StackPanel>
                  <TextBlock Text="Serial" Foreground="#8A94A0" FontSize="11"/>
                  <TextBlock Text="{Binding SerialNumber}"/>
                </StackPanel>
              </StackPanel>
            </Grid>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
  </Grid>
</UserControl>
